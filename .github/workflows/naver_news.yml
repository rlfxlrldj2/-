name: Naver News Daily 08:00 KST
on:
  workflow_dispatch:
  schedule:
    - cron: "0 23 * * 0"   # 일 23:00 UTC = 월 08:00 KST
jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "✅ Workflow visible smoke test"
